package edu.ccrm.config;

public class AppConfig {
    private static AppConfig instance = null;
    private AppConfig() {}
    public static synchronized AppConfig getInstance() {
        if (instance == null) instance = new AppConfig();
        return instance;
    }
    // config fields, getters
}
